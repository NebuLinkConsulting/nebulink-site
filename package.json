{
  "name": "nebulink-site",
  "version": "0.0.0",
  "private": true,
  
  "scripts": {
    "dev": "astro dev",
    "start": "astro dev",
    "build": "astro build",
    "preview": "wrangler pages dev ./dist",
    "astro": "astro",
    "generate-types": "wrangler types",
    "deploy": "astro build && rmdir /s /q .wrangler & cd dist\\server && wrangler deploy"
  },
  "devDependencies": {
    "wrangler": "^4.92.0"
  },
  "dependencies": {
    "@astrojs/cloudflare": "^13.5.1",
    "@types/turndown": "^5.0.6",
    "astro": "^6.3.3",
    "fs-extra": "^11.3.5",
    "resend": "^6.12.3",
    "turndown": "^7.2.4"
  },
  "overrides": {
    "vite": "^7"
  }
}